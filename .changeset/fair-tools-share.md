---
"@aliou/pi-processes": patch
---

Match background process commands to Pi's inherited environment, and coordinate process widgets and the `/ps` dashboard through the installed `pi-extension-utils` host, including the shared navigation legend and collapsible sidebar. Avoid loading a second bundled host when `pi-extension-utils` is installed as a shared Pi package.
